{:name "My Playbook"
 :hosts "all"
 :vars {:greeting "Hello from NPKM!"}
 :tasks
 [{:name "Say hello"
   :debug {:msg "{{ greeting }}"}}
  {:name "Ensure deploy dir exists"
   :file {:path "{{ deploy_dir }}" :state "directory"}}]}
